import styled from '@emotion/styled';

export const ButtonUnStyled = styled.button`
  appearance: none;
  font: inherit;

  &,
  &:active,
  &:focus,
  &:hover {
    background: none;
    border: none;
    outline: none;
  }
`;
