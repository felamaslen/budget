import styled from '@emotion/styled';

export const ButtonUnStyled = styled.button`
  appearance: none;

  &,
  &:active,
  &:focus,
  &:hover {
    background: none;
    border: none;
    outline: none;
  }
`;
