import styled from 'styled-components';

import { Flex } from '~/styled/layout';

export const Main = styled(Flex)`
  display: flex;
  flex-flow: column;
  position: absolute;
  left: 0;
  right: 0;
  top: 0;
  bottom: 0;
  overflow: hidden;
`;
