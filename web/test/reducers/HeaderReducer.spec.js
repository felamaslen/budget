import { expect } from 'chai';

import { getNavRow, getNavCol } from '../../src/reducers/HeaderReducer';

describe('HeaderReducer', () => {
    describe('getNavRow', () => {
        it('should get the correct row');
    });

    describe('getNavCol', () => {
        it('should get the corret column');
    });
});

