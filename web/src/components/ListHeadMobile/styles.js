import styled from 'styled-components';

export const ListHead = styled.div`
    display: flex;
    flex: 0 0 28px;
    align-items: center;
    flex-flow: row nowrap;
    width: 100%;
    font-weight: bold;
    user-select: none;
`;
