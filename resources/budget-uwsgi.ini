[uwsgi]
  base = ./srv

  app = budget
  module = %(app)

  home = %(base)
  virtualenv = %(base)/../env
  pythonpath = %(base)

  socket = %n.sock
  chmod-socket  = 664

  callable = APP

  logformat = req: %(method) %(addr) -> %(status)
  logto = /var/log/uwsgi/%n.log
